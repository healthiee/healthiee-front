const mobileSize = {
  mobile: `screen and (max-width: 360px)`,
};

const fontSize = {
  sm: '12px',
  md: '16px',
  lg: '20px',
  xlg: '24px',
};

const fontWeight = {
  regular: 400,
  bold: 700,
};

const colors = {
  gray: '#717171',
  lightGray: '#EFEFEF',
  red: '#FF0000',
  lightRed: '#FFE0E0',
  orange: '#FF9F4E',
  lighOrange: '#FFC493',
};

const theme = {
  mobileSize,
  fontWeight,
  fontSize,
  colors,
};

export default theme;